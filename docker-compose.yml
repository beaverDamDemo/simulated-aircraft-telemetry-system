services:
  backend:
    build: ./backend
    container_name: telemetry-backend
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: telemetry-frontend
    ports:
      - '4200:80'
    restart: unless-stopped
